# Cloudflare Pages configuration
# Docs: https://developers.cloudflare.com/pages/configuration/build-configuration/

name = "medcorebd"
compatibility_date = "2024-09-23"

# Pages build configuration
[env.production]
# Environment variables are set in the Cloudflare Pages dashboard, not here.
# Sensitive keys (API keys, secrets) should NEVER be in this file.

[env.preview]
# Preview deployments (pull requests) use these overrides.
# You can set branch-specific vars in the CF Pages dashboard too.
