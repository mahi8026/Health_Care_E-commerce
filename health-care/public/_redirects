# Cloudflare Pages redirects
# Docs: https://developers.cloudflare.com/pages/configuration/redirects/
#
# NOTE: Category query-param redirects (e.g. /products?category=X → /products/category/slug)
# are handled by Next.js redirects in next.config.mjs — Cloudflare Pages does not
# support query-parameter-based redirect rules, so they are intentionally omitted here.
