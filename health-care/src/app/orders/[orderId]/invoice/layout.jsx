/**
 * Invoice Layout
 * Minimal layout without site chrome (header/footer/nav)
 * for clean invoice printing and display
 */
export default function InvoiceLayout({ children }) {
  return <>{children}</>;
}
